[
  {
    "name": "${family}",
    "image": "${image_uri}",
    "essential": true,
    "portMappings": [
      { "containerPort": ${container_port}, "protocol": "tcp" }
    ],
    "environmentFiles": [
      { "type": "s3", "value": "${env_bucket_arn}/${env_file_name}" }
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${awslogs-group}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "ecs"
      }
    }
  }
]
